[package]
name = "dynwinrt-com-contracts"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Native-independent reviewed contracts for bounded dynwinrt COM copies"
repository = "https://github.com/microsoft/dynwinrt"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
