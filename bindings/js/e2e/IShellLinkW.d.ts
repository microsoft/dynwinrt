// Generated by dynwinrt-codegen — do not edit
import { SHOW_WINDOW_CMD } from './SHOW_WINDOW_CMD.js';
import type { DynWinRtValue } from '../dist/index.js';

/** Opaque Win32 handle. Pass either a raw pointer value as a `bigint` (safe for full 64-bit handle values) or a `number` (only for handles that fit in a JS safe integer, e.g. HWND with small window IDs). Do NOT pass a `Buffer` — `DynCom.pointer(Buffer)` uses the buffer's own address, not the bytes it contains; for an Electron `getNativeWindowHandle()` Buffer, read the handle value first (for example, `buf.readBigUInt64LE(0)`). */
export type HWND = bigint | number;
/** Win32 NUL-terminated string pointer. Pass a `Buffer` holding the string bytes (including the NUL terminator), or pass a raw pointer as `bigint`. */
export type PWSTR = bigint | Buffer;

export declare const IID_IShellLinkW: unknown;

export declare class IShellLinkW {
    /** Wrap an existing native COM pointer (for QueryInterface bridging). */
    static _fromNative(obj: unknown): IShellLinkW;
    getPath(cch?: number, pfd?: bigint | Buffer, fFlags?: number): string;
    getIDList(): DynWinRtValue;
    setIDList(pidl: bigint | Buffer): void;
    getDescription(cch?: number): string;
    setDescription(name: PWSTR): void;
    getWorkingDirectory(cch?: number): string;
    setWorkingDirectory(dir: PWSTR): void;
    getArguments(cch?: number): string;
    setArguments(args: PWSTR): void;
    getHotkey(): number;
    setHotkey(wHotkey: number): void;
    getShowCmd(): SHOW_WINDOW_CMD;
    setShowCmd(iShowCmd: SHOW_WINDOW_CMD): void;
    getIconLocation(cch?: number): [string, number];
    setIconLocation(iconPath: PWSTR, iIcon: number): void;
    setRelativePath(pathRel: PWSTR, reserved: number): void;
    resolve(hwnd: HWND, fFlags: number): void;
    setPath(file: PWSTR): void;
}
